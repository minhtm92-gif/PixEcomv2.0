{
  "name": "pixecom-v2",
  "version": "0.1.0",
  "private": true,
  "description": "PixEcom v2 — Multi-tenant Seller Portal",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "db:generate": "turbo db:generate",
    "db:push": "turbo db:push",
    "db:migrate": "turbo db:migrate",
    "db:studio": "pnpm --filter @pixecom/database db:studio",
    "seed:staging": "pnpm --filter @pixecom/database seed:staging",
    "seed:staging:reset": "pnpm --filter @pixecom/database seed:staging:reset",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.1.0"
  },
  "packageManager": "pnpm@9.1.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
